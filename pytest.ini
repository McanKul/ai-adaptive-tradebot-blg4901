[pytest]
asyncio_mode = auto
markers =
    asyncio: marks an async test (handled by pytest-asyncio)
    slow: marks a slow test
filterwarnings =
    ignore::DeprecationWarning:websockets.*
    ignore::DeprecationWarning:binance.*
testpaths = tests
